# Legacy Migration Data

`problems.csv` and `attempts.csv` are bundled source data for the one-time
`kanji_test_generator` migration performed by `storage.ts`. Keep them alongside
the migration while existing browser profiles may still need the import.

`local-backups/` holds pre-migration or pre-fix CSV snapshots kept on the local
machine only. Its contents are intentionally ignored by Git.
